local wezterm = require "wezterm"

local config = {
	color_scheme = "vscode-dark",
  {{ if eq .chezmoi.os "darwin" }}
  color_scheme_dirs = { "/Users/{{ .chezmoi.username }}/.config/wezterm/colorschemes" },
  {{ else if eq .chezmoi.os "linux" }}
  color_scheme_dirs = { "/home/{{ .chezmoi.username }}/.config/wezterm/colorschemes" },
  {{ end }}
  enable_tab_bar = false,
	font = wezterm.font("VictorMono Nerd Font", { weight = "DemiBold" }),
  {{ if eq .chezmoi.os "linux" }}
	font_size = 13,
  {{ else }}
  font_size = 18,
  {{ end }}
	window_decorations = "NONE"
}

return config
